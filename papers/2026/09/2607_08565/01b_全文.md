# 全文

> source: `arxiv-html`

SMetric: Rethink LLM Scheduling for Serving Agents with Balanced Session-centric Scheduling Report GitHub Issue × Title: Content selection saved. Describe the issue below: Description: Submit without GitHub Submit in GitHub arXiv is now an independent nonprofit! Learn more &times; Back to arXiv Why HTML? Report Issue Back to Abstract Download PDF Abstract 1 Introduction 2 Background and System Setup 3 Agentic serving patterns in the wild 3.1 KV$ reuse patterns 3.2 Session execution patterns 4 Balanced session-centric scheduling 4.1 Analysis of existing schedulers 4.2 Our approach 5 Performance evaluation 5.1 Evaluation setup 5.2 End-to-end serving performance: PD-colocation 5.3 End-to-end serving performance: PD-disaggregation 5.4 Impact of pre-filter routing 5.5 Sensitivity to pre-filter routing parameters 6 Related Work 7 Conclusion References License: arXiv.org perpetual non-exclusive license arXiv:2607.08565v1 [cs.DC] 09 Jul 2026 SMetric : Rethink LLM Scheduling for Serving Agents with Balanced Session-centric Scheduling Jiahao Wang Affiliation: Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University Kaizhan Lin † Affiliation: Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University Kaixi Zhang Affiliation: Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University Jinbo Han Affiliation: Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University Xingda Wei ✉ Affiliation: Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University Sijie Shen Affiliation: Alibaba Group Chenguang Fang Affiliation: Alibaba Group Wenyuan Yu Affiliation: Alibaba Group Rong Chen Affiliation: Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University Haibo Chen Affiliation: Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University Abstract LLM scheduling is critical to serving, yet it remains unclear how well existing designs fit agentic serving—with LLM requests issued by agents instead of humans. This shifts the workload in two ways: (1) agents act only on complete responses, making the cluster’s tokens per second (TPS) the primary goal and relaxing—not eliminating—per-token latency requirements; and (2) requests share much of their KV$—reuse exceeds 80 % of request tokens in a production trace from Bailian , versus 54–62 % in chat. This paper first contributes a systematic study of request scheduling for agents on two real-world traces. We find that to increase KV$ reuse, existing schedulers overly prioritize routing requests to instances caching their KV$, overloading a few while leaving the rest idle, capping TPS. We thus present two key insights: (1) load balance need not sacrifice all KV$ reuse, thanks to the global-tier KV$ store and (2) by utilizing the workload’s intra-session locality, balancing a small fraction of requests—the first request in each agent session—suffices to balance the cluster without sacrificing most KV$ reuse on local instances. SMetric realizes these insights with balanced session-centric scheduling: it routes each session’s first request purely for load balance and its follow-up requests in a cache-aware manner, preserving load balance and local reuse while keeping demand on the global tier low. Using the session turn information as the scheduling metric is deliberate: it is derived efficiently and accurately from the user inputs alone, so the scheduler stays clean and stateless. SMetric improves cluster TPS by 10–16 % under prefill-decode colocation with a global store and prefill TPS by 2–34 % under disaggregation over state-of-the-art schedulers, also with a better per-token latency. † † footnotetext: Kaizhan Lin is affiliated with ShanghaiTech University; this work was done while he was an intern at Institute of Parallel and Distributed Systems, Shanghai Jiao Tong University. ✉ ✉ footnotetext: Xingda Wei is the corresponding author ( wxdwfc@sjtu.edu.cn ). Introduction Large language models (LLMs) now power agents like Claude Code [ 6 ] and OpenClaw [ 36 ] , which accomplish complex tasks such as writing [ 5 ] , debugging [ 14 ] , and tuning the performance [ 39 , 32 ] of the code that drives the computing world. An agent solves a task through a session of LLM requests, and generated tokens drive the agent toward completing its task. Therefore, how efficiently a model provider serves these tokens determines both the speed and the cost of running agents. To that end, existing providers deploy clusters dedicated to serving requests from many users’ sessions, each cluster running many instances, each able to serve a complete request. We term this cluster-scale, agent-dedicated LLM serving agentic serving in this paper. Note that agentic serving is still LLM serving: the provider serves each request through the standard LLM API and neither runs the agents nor sees their internal logic. What changes is the workload, because the requests now come from agents. Request scheduling—how the cluster routes each request to an instance—is a key pillar of LLM serving. Though well-studied recently [ 54 , 59 , 42 , 53 , 17 , 2 ] , we argue that current scheduling designs must be revisited for agentic serving, for two reasons: First, agentic serving requires a different performance objective: the cluster’s tokens per second (TPS) becomes the primary goal, while the per-token latencies targeted by prior work [ 54 , 59 , 58 ] —time-to-first-token (TTFT) and time-per-output-token (TPOT)—still cannot be sacrificed, only relaxed. This shift is driven by two properties of agentic workloads. (1) An agent emits far more tokens per request than a human chat user does [ 9 ] , so a cluster must sustain high TPS to serve the load at low cost. Besides cost, a cluster whose TPS falls short of demand queues requests, so every agent waits longer for its response. (2) An agent tolerates delays on any individual token—the first or a later one—as long as the complete response arrives quickly: unlike a human who reads tokens as they stream [ 8 ] , an agent acts only on the complete response, e.g., to decide which tool to call. Latency requirements still hold, however: a response that is too slow makes the agent treat the request as timed out [ 7 ] ; these requirements are only more relaxed than for humans. Second, agentic serving exhibits a different pattern from chat: KV$ reuse dominates. Specifically, the efficiency of serving an LLM request depends heavily on KV$ reuse, where the KV$ is the intermediate state a cluster caches to avoid recomputation (§ 2 ). In chat workloads, this reuse is modest: only 54–62 % of request tokens are reused from cache [ 45 ] . In agentic serving, KV$ reuse dominates: by analyzing the production trace from a cluster at one of the world’s largest token providers ( Bailian ), we find reuse exceeds 80 % (Finding 3.1 ). Throughout, we mark such key empirical observations as numbered Findings , all presented in § 3 . This shift requires revisiting whether current scheduling designs remain effective when KV$ reuse dominates at this scale. In this paper, we conduct the first systematic study of request scheduling for agents, using two recent large-scale real-world agentic serving traces (§ 3 ). By studying both state-of-the-art schedulers and the production scheduler of Bailian , we find that the key issue is not that they fail to reuse the KV$, but that they overly prioritize it: they route each request to the instance holding its cached KV$ (cache-aware), trading load balance for reuse. In particular, first-turn requests across sessions of the same agent framework share similar system prompts (Finding 3.1 ), so a cache-aware scheduler routes them all to the few instances caching these prompts. Moreover, since each follow-up request is likely routed to the instance caching its session’s earlier turns, every session is pinned to the instance where its first request lands; these few instances thus absorb entire sessions and become overloaded, while the rest of the cluster sits underutilized. Sacrificing load balance is harmful: an imbalanced cluster leaves the compute of underloaded instances idle, directly capping TPS. Worse, the sacrifice is unnecessary, as we find the token computation of real-world agentic workloads can be balanced across instances at scale (Finding 3.2 ). Meanwhile, simply abandoning cache-awareness for balance is no better, since reuse saves the computation of most prefill tokens, as discussed above. To address this dilemma, we draw two insights. First, from the system’s perspective, pursuing load balance does not have to compromise KV$ reuse: modern agentic serving adopts a two-tier KV$ store—a local tier on each instance’s GPU memory and a global tier on cluster-wide CPU memory (§ 2 ). Even if the scheduled instance does not hold the KV$ locally, it can fetch it from the global tier. As long as the global tier serves these KV$ fetches in time, TPS is unaffected (§ 4.1 ); the only cost is a small increase in TTFT, which agents tolerate as discussed above. The global tier, however, is not a free lunch: its capacity and bandwidth are limited, and routing every request purely for balance—funneling all reuse through global fetches—risks exceeding what it can hold and serve in time. The scheduler should therefore still exploit local-tier reuse. Second, from the agent workload’s perspective, once the scheduler considers local-tier KV$ reuse—i.e., prioritizes routing targets with high reuse, the common practice—the placement of each session’s first request largely determines the cluster’s load balance. This is because of the workload’s intra-session locality: (1) follow-up requests mostly reuse the KV$ of earlier turns in the same session (Finding 3.1 ), and (2) such reuse happens quickly (Finding 3.1 ), so when a follow-up request arrives, its KV$ typically still resides in the local tier of the instance that served the previous turn. Consequently, all requests of a session are likely routed to the same instance as its first request (§ 4.2 ). Scheduling first requests alone for load balance thus suffices to balance the cluster; and since the first requests of sessions are only a small fraction of all requests, the global tier alone is sufficient to support their KV$ reuse. Based on these two insights, we propose SMetric : balanced session-centric scheduling for high-TPS agentic serving. SMetric incorporates agent session information into scheduling with a differential policy: for the first request of each session, it schedules purely for load balance, spreading sessions across all instances of the cluster; for follow-up requests, it follows traditional cache-aware routing that prioritizes local-tier reuse, keeping demand on the global tier low. Using the session-turn information as a routing hint is our explicit design choice: this hint can be obtained in a router-stateless way from an existing LLM call, i.e., without the router keeping any record of users’ past requests. The router thus routes each request as a pure function of the request itself, keeping no per-session state to track or to reclaim as sessions go idle, which keeps the router simple and scalable. This simple policy also achieves balanced execution while preserving KV$ reuse, even for the first requests: their reuse comes mostly from the shared system prompt of the agent framework (Finding 3.1 ), which the scheduled instance can fetch from the global tier wherever the request lands. Finally, to handle the rare tail case where many long sessions accumulate on the same instance, SMetric migrates sessions across instances upon imbalance. We have implemented SMetric on vLLM [ 44 ] and LMCache [ 29 ] , and extensively evaluated it against state-of-the-art schedulers, including the production scheduler used in Bailian , with serving paradigms spanning both prefill-decode colocation and prefill-decode disaggregation. SMetric serves 10–16 % more TPS under colocation when the global store is present, and 2–34 % more prefill TPS under disaggregation than the best-performing baseline across global-tier provisionings. It also lowers the P50 and P90 TPOT under colocation and the TTFT at all reported percentiles under disaggregation (37 % lower at the median). In summary, this paper makes the following contributions: • The first study of the workload patterns of LLM requests served by an agent-dedicated cluster at scale (§ 3 ). • The first analysis of how existing schedulers perform with the two-tier KV$ store under agentic serving (§ 4.1 ). • A new LLM scheduling design that incorporates the session characteristics of agent requests to achieve both high KV$ reuse and load-balanced serving (§ 4.2 ). • Evaluation that confirms the effectiveness of SMetric (§ 5 ). We will open-source SMetric as well as traces upon publication. Background and System Setup Figure 1 : The KV$ store hierarchy for serving agents. LLM and KV$. An LLM serves a request in two phases. Given the input tokens (the prompt ), a prefill pass processes the prompt with a single model forward (possibly split into chunks [ 1 ] ) and emits the first output token. A decode pass then emits the remaining tokens one by one in an auto-regressive manner, until the model produces an end-of-sequence (EOS) token. Each token comes from a single model forward whose input is the prompt followed by every token emitted so far, appended in generation order. The forward pass of LLMs requires computing a key (K) and a value (V) tensor for each token in the input sequence. Since these tensors are the same for inputs with the same prefix, it is standard to cache the KV tensors generated so far (the KV$) to accelerate the decode phase, as well as the prefill phase of later requests with the same prefix. As shown in Figure 4 (a), if a 3-token input request has two KV$ hits, then it only needs to compute the KV$ for one token. KV$ store hierarchy and the local vs. global tiers. Since cross-request reuse is common in production workloads [ 45 , 38 ] , serving systems typically cache the KV$ of finished requests for some time. Caching requires storage capacity because each request’s KV$ is large: for example, a single 128K-token request consumes about 32 GB of KV$ on the popular Qwen3-32B model. As a result, the KV$ store in production systems follows a multi-tier architecture as shown in Figure 1 . The first tier is device (GPU) memory; when it fills, KV$ cascades to the larger, global tiers below—host (CPU) memory, then cloud storage (e.g., S3). A later KV$ hit on a lower tier must load the entry back up to the GPU. Note that thanks to recent techniques including GPU-direct RDMA, the global tier can also fetch data from remote GPU memory (➂). Figure 2 : The detailed architecture of global-tier KV$. The global tier enables a GPU to read KV$ from local CPU (➀), remote CPU memory (➁) and remote GPU memory (➂). In this paper, we consider only two tiers: the GPU memory tier (local tier) and a CPU memory tier linked by RDMA (global tier). This does not lose generality: the storage tier shares the same PCIe and RDMA link as the computation tier [ 47 ] , so modeling it separately would not increase the speed of the global tier. Figure 3 : An example of how agents interact with the served LLM. Agentic workflow and the agent session. Modern LLM agents such as Claude Code [ 6 ] solve a task through a session of multiple rounds of interaction with the LLM. As shown in Figure 3 , the first request (Request #1) in a session, typically written by the user, specifies the task to solve. The LLM replies with tokens that carry its reasoning and the next step for the agent, such as a tool call that reads a file with cat (Reply #1). The agent then runs the tool, appends the tool call output to the context, and issues the next request (Request #2). Each request resends the whole history because the LLM itself keeps no state across requests. This append-only pattern—request i i extends request i − 1 i{-}1 —also lets request i i hit the KV$ of request i − 1 i{-}1 (if the platform has not evicted it), which makes serving more efficient. LLM serving architecture in the wild. To serve LLM requests at scale, providers typically deploy each model on a cluster of GPUs 1 1 1 The model provider could deploy multiple clusters for serving, and inter-cluster scheduling is beyond the scope of this work. . The smallest unit of deployment is a model instance —one or more GPUs that together hold a full copy of the model parameters. A global router dispatches each incoming request to one instance using a routing algorithm [ 43 , 42 , 59 , 54 ] . Because a request runs in two phases—prefill and decode—there are two mainstream serving architectures today: Figure 4 : (a) An overview of the prefill and decode of LLM processing with KV$. (b) The PD-colocated and (c) the PD-disaggregated serving architecture. PD colocation. PD colocation runs the prefill and decode of a request to completion on the same instance (Figure 4 (b)). PD disaggregation. PD disaggregation uses two clusters for serving: one cluster only runs prefill and the other only runs decode [ 58 , 37 ] (Figure 4 (c)). A request first runs prefill on a prefill instance, which transfers the resulting KV$ over RDMA to a decode instance, where the decode phase runs to completion. The vendor typically deploys two clusters for ease of management. Both architectures build atop the same KV$ hierarchy of Figure 1 ; they differ only in which instances access the global tiers. Cross-request KV$ reuse is needed at prefill, so the access pattern follows which instances run prefill. Under PD colocation, every instance runs prefill, so all of them must access the global tiers for reuse; under PD disaggregation, only prefill instances run prefill, so only they access the global tiers. Agentic serving patterns in the wild Analyzed traces. To understand the workload characteristics of real-world agentic serving, we collected two recent representative traces from two large-scale clusters, both dedicated to serving agents for a commercial coding-agent product (a paid subscription plan). Each cluster is among many clusters supporting the product, and contains at least thousands of accelerators. More specifically, both traces were collected during the same peak usage hours (10:00–12:00) on May 29, 2026. One trace serves a popular TB-level model (Trace 1) and the other a roughly 280B-parameter model (Trace 2), covering agents with different capabilities. To study the workload in depth and to evaluate the serving system under the studied workloads, we collected as much information as possible within Bailian ’s privacy policy. Each trace contains the following information: for each LLM request, we collected (1) the hashed content of the request—which lets us analyze how much a request can benefit from KV$ reuse, (2) the arrival time of the request (that captures the burstiness of requests and the reuse interval of KV$), (3) the session information of each request—including the session ID, the parent request ID, the turn number and how the request is triggered (e.g., by a human or by the agent). Figure 5 : A characterization of the reusable KV$ for all requests, with a breakdown of where the reuses come from. KV$ reuse patterns We first analyze the following key features of KV$ usage in agentic serving: the KV$ reuse ratio, where the reuse comes from, and the distribution of reuse intervals. We distilled four findings through these metrics: Finding 1: KV$ reuse is dominant : the reuse ratio exceeds 80 % of the KV$ in both traces (Figure 5 ). Figure 5 profiles the workload KV$ reuse ratios in agentic serving scenarios: we can skip 80 % of the KV$ computation by reusing those generated so far, assuming all KV$ can be stored. We measure reuse at KV-cache block granularity using an idealized infinite KV store, where each block holds a fixed number of tokens, with the block size matching that used by the cluster serving the workloads. During prefill, each KV block is looked up in the store; hit blocks contribute the tokens whose computation can be skipped. The reusable ratio is the number of skipped prefill tokens divided by the total number of prefill tokens. Finding 2: KV$ reuse follows session locality : more than 65 % of reuses come from follow-up requests (the second turn onward) reusing earlier turns of the same session (Figure 5 ). To see where the reuse comes from, for each reuse we track which session originally produced the reused tokens. Figure 5 breaks down the reuse on this basis. We split reuse into two categories: intra-session reuse, when a request’s reused part comes from a previous request in the same session, and inter-session reuse, when it comes from a request in a different session. Intra-session reuse contributes most of the reuse (roughly 67 % in both traces), all of it from follow-up requests that reuse earlier turns of the same session, which is unsurprising: different sessions tend to solve different problems, so cross-session reuse is rare. Note that this intra-session reuse ratio is a lower bound on session locality: some reuse we attribute to inter-session could equally be served from within the same session. For example, we observe that much inter-session reuse stems from multi-session workflows [ 4 , 33 ] in which an agent forks multiple agents to solve a task. The sessions of the forked agents naturally share the context of the parent session. Finding 3: First-turn requests enjoy KV$ reuse : the shared system prompt accounts for 18–20 % of all reuses (Figure 5 ). Figure 6 : Reuse-time distribution across the two production traces. Most KV$ blocks are reused quickly: roughly 90% of all reuse occurs within about 100 seconds, while the long tail extends to hours. The tail is truncated by the 2-hour trace collection window. A request that starts a fresh session has no earlier turn in the same session to reuse, yet it still benefits from reuse through the system prompt—a special sub-case of inter-session reuse where the match is the shared prompt template. As shown in Figure 5 , system-prompt reuse contributes 18 % and 20 % of the reuse in Trace 1 and 2, respectively. The reuse comes from the fact that many requests are triggered by the same agent framework, e.g., Claude Code [ 6 ] or OpenClaw [ 36 ] , which uses a similar prompt template for the first request in each session. Figure 7 : A breakdown of what triggers the requests in the two traces. Agent denotes requests automatically sent by the agent; user denotes requests sent by the human; compaction is a special agent-triggered request that summarizes the context of the current session. Finding 4: KV$ is quickly reused : about 90 % of all reuses recur within roughly 100 s after the last request that shares the KV$, with only a small tail (Figure 6 ). Figure 6 plots the distribution of the reuse interval—the time from when a request completes until its KV$ is next reused. Most reuses happen within hundreds of seconds, often within a few seconds. This is because most reuses are triggered by agents rather than humans, and agents have a short feedback loop. Figure 7 profiles the sources of requests across the two traces; both are dominated by agent-triggered requests, which means that the agent automatically triggers LLM requests based on the previous responses and tool call results (see the example in Figure 3 ). Finally, though most reuses happen quickly, some outliers cause KV$ to be reused hours later. We suspect this happens when a user forgets to respond to the agent’s request in time. Session execution patterns Next we examine the detailed pattern of each session and derive the following two key findings: Figure 8 : An analysis of token usage of different sessions. Finding 5: The session token usage is skewed : in both traces, the top 25 % of sessions contribute more than 80 % of the tokens (Figure 8 ). As shown in Figure 8 , the computation required varies significantly across sessions, with a small fraction of sessions accounting for most of the tokens to compute. Nevertheless, due to the large overall request volume, even 3 % of the requests arriving within one minute still exceed the total number of serving instances. Specifically, the ratio of requests arriving within a 1-minute window to the total serving instances is on average 43 (Trace 1) and 48 (Trace 2), and never drops below 30 in either trace. Thanks to the large request scale, even though sessions have skewed token usage, their computation can be balanced across all instances: Finding 6: Despite the skewness, the tokens of sessions can still be balanced across serving instances over time. Figure 9 : Per-window token load imbalance (max/mean) across instances under load-balancing dispatch, for two traces. To quantify this, we use a trace-driven simulation that replays all requests of a trace in arrival order on a simulated cluster with the same number of instances as the traced cluster. The simulator dispatches each request to the instance currently holding the fewest in-flight tokens, counting all tokens—prefill and decode—of the requests running on that instance, and the request stays at the instance for its execution time recorded in the trace. Figure 9 shows the resulting distribution of per-window load imbalance during the simulation. Specifically, for each time slice (one minute), we count the imbalance as the token count of the instance with the maximum load divided by the mean token count across all instances (max/mean). We can see that the load stays largely balanced across instances: the median per-window imbalance is only 1.7 for Trace 1 and 1.5 for Trace 2, and even the worst window stays below 2.4 and 2.1, respectively. Note that the simulation is approximate: a request’s execution time on the simulated cluster may differ from that on the original cluster. Nevertheless, it suffices to reveal the workload’s balance potential, and our analysis in § 4.1 further confirms that schedulers balancing the load alone keep instance loads balanced. Balance holds because concurrent requests far outnumber instances: even when token-heavy requests occupy a subset of instances, the remaining instances absorb many token-light ones, which evens out the skew of individual sessions. Such a high request-to-instance ratio is common in serving: a GPU instance requires a large batch of requests to achieve high utilization, and requests are not scarce for large service providers like Bailian . Balanced session-centric scheduling Analysis of existing schedulers ⬇ 1 req = receive () 3 c = kvc . hit_len ( req . prompt ) # KV$, per instance 4 l = load () # load, per instance 6 sched_to = instances . argmin / max ( f ( c , l )) 7 req . forward ( sched_to ) Figure 10 : A simplified view of how existing LLM schedulers route requests at the cluster router. f ⁡ ( c i , l i ) f(c_{i},l_{i}) is the scheduling score, where c i c_{i} captures KV$ reuse and l i l_{i} captures load for an instance i i . Methods differ in how they define f f . A preliminary on the LLM scheduling in a cluster. To the best of our knowledge, LLM schedulers [ 54 , 38 , 17 , 2 , 34 , 3 , 42 , 59 , 53 , 11 ] follow the same methodology as traditional request routing: upon receiving a request, the scheduler computes a score for each instance i i and routes the request to the instance with the lowest (or highest) score, depending on the score definition. Figure 10 illustrates the methodology. Specifically, for each instance to route, its score is defined as f ⁡ ( c i , l i ) f(c_{i},l_{i}) over two per-instance indicators: c i c_{i} estimates how much of the request’s prefill can be skipped by reusing instance i i ’s locally cached KV$ (e.g., the prefix hit length), and l i l_{i} estimates the instance’s load (e.g., its batch size or the number of inflight tokens). Our analysis uses the following representative methods: • Bailian is the production method currently deployed at Bailian . It uses a linear-combined scoring function of KV$-awareness (the hit ratio) and the current load (batch size). Other popular open-source schedulers adopt a similar design [ 34 , 3 ] . Specifically, the f f is defined as f = ( 1 − λ ) ⋅ l ^ i + λ ⋅ ( 1 − c ^ i ) f=(1-\lambda)\cdot\hat{l}_{i}+\lambda\cdot(1-\hat{c}_{i}) (the lower the better), where c ^ i \hat{c}_{i} is the KV$ hit ratio and l ^ i \hat{l}_{i} is the batch size of the instance, and λ \lambda is a tuned weight that trades KV$ reuse for load balance. Note that each indicator carries a ⋅ ^ \hat{\cdot} that normalizes it to a common range; otherwise the two indicators cannot be added together. For serving agentic workloads, the operator explicitly sets a large λ \lambda to favor more KV$ hits. • LMetric [ 54 ] is the state-of-the-art scheduler published recently, whose effectiveness has been verified on non-agentic workloads. It uses a multiplication-based combinator to combine the KV$-awareness and load balance indicators: f = ( L − c i ) × l i f=(L-c_{i})\times l_{i} , where L L is the request’s prompt length, so L − c i L-c_{i} is the prefill computation left after reuse. The multiplication eliminates the tuned weight while preserving both KV$-awareness and load balance. • Load-balance-only considers load only, i.e., f = l i f=l_{i} , routing each request to the least-loaded instance regardless of its KV$ reuse. It is a variant of the classic join-the-shortest-queue policy used in traditional cluster-level request routing and the default policy of vLLM [ 44 ] , a popular LLM serving framework. The analysis methodology. We compare the performance of scheduling methods by replaying our production trace (Trace 1) on a 32-instance vLLM [ 44 ] cluster serving Qwen3-Coder-30B-A3B (detailed configurations in § 5.1 ). Since the evaluated model generates responses that differ from those recorded in the trace, a strawman replay would distort the request patterns. We therefore carefully adjust the responses during replay to ensure a consistent trace execution, e.g., forcing the model to generate the same number of output tokens as recorded in the trace (detailed in § 5.1 ). The cluster uses Mooncake [ 38 ] with LMCache [ 29 ] , the best-performing open-source stack to our knowledge, as its global KV$ store. We carefully tuned each system to its peak performance: for example, under microbenchmarks, KV$ fetches from the global tier saturate the cluster’s RDMA bandwidth. We only replay Trace 1: the two traces exhibit similar patterns, as we have analyzed in § 3 , so we expect similar serving performance on Trace 2. We only report the results of one model served under PD colocation here; the results of PD disaggregation are similar and are presented in § 5 . We report each method’s maximum TPS within SLO . For each offered request rate, TPS within SLO is the cluster-wide TPS contributed only by requests that satisfy both the TTFT and TPOT SLOs. As discussed in the introduction, the request-level SLO remains important for agents: although an agent tolerates fluctuations in per-token latency, a response that is too slow makes the agent treat the request as timed out [ 7 ] . We set the SLO following Bailian ’s production targets, which also match the common practice in academic studies [ 58 , 46 , 55 ] : a TTFT SLO linear in the number of input tokens and a fixed TPOT SLO (30 ms). We sweep the offered request rate and take the maximum TPS within SLO over the sweep. Finally, we also analyze the performance with different global tier provisioning. We call the global tier fully provisioned when it is configured with the maximum capacity of our testbed. For the model analyzed in this section, the fully provisioned global tier is never the bottleneck: it holds the KV$ of all ongoing sessions, and its bandwidth keeps up with the fetch demand. Nevertheless, in production we do observe under-provisioned global tiers, due to either insufficient capacity or bandwidth. We therefore additionally sweep the global tier’s per-node capacity, relative to the local tier, and report each method’s performance under each provisioning. Figure 11 : (a) Analysis of the TPS for different schedulers with different global-tier offerings. (b) The KV$ reuse ratio and where the reuse comes from. The red blocks use the fully provisioned global-tier setup. (c) An analysis of the load balancing of different methods. Load balance does not always trade KV$ reuses. Counterintuitively, the load-balance-only method—completely KV$-agnostic at scheduling time—recovers most of the lost TPS once the global tier is fully provisioned, as shown in Figure 11 (a): it is within 7 % of the strongest baseline. Figure 11 (b) explains why: with a fully provisioned global tier (red bars), all methods reach a nearly identical reuse ratio (73–75 %), because the KV$ missed in the local tier is fetched from the global tier. The methods differ only in where the reuse comes from: load-balance-only fetches most of its reused KV$ from the global tier (hatched), while Bailian hits mostly in the local tier (solid). KV$-aware methods do not substantially outperform the load-balance-only method, because KV$-awareness introduces a subtle trade-off—increased load imbalance that hurts TPS. Figure 11 (c) profiles the per-instance load: the tokens each instance processes per minute during the first 10 minutes of the replay, with the global tier fully provisioned. We quantify load imbalance as the mean, over time, of the ratio between the hottest instance’s load and the mean instance load; 1.0 × \times means perfect balance. Load-balance-only keeps the load the most uniform (2.1 × \times ), while LMetric and Bailian are more skewed (3.0 × \times and 2.7 × \times , respectively): they route requests toward instances with higher KV$ hits, even though a large fraction of the KV$ could instead be fetched from the global tier. This skew also appears visually: the dark cells—overloaded instances—concentrate in the KV$-aware heatmaps. Load balance alone is still insufficient. The load-balance-only method still does not outperform the others: although it achieves better GPU utilization, it stalls waiting to fetch KV$. It falls behind especially when the global tier is limited or absent, so KV$-awareness is still critical because reuse saves substantial prefill computation, consistent with existing studies [ 54 , 38 ] . With no or a small global tier, Bailian achieves the highest TPS of all methods (Figure 11 (a)), because it preserves a high local-tier hit ratio: 58 %, vs. 33 % for LMetric and 6 % for load-balance-only (the blue bars in Figure 11 (b)). LMetric is slower than Bailian on agentic workloads because it favors load balance and sacrifices KV$ reuse. For production serving, a scheduler should therefore balance the load while tolerating an under-provisioned global tier. Such under-provisioning is possible: we do observe it in production; other hardware may have a smaller global-to-local capacity ratio than our evaluated machines; and a larger model puts more pressure on the global tier, which we also observe on our testbed (§ 5.2 ). Our approach Figure 12 : Session stickiness to the first-turn instance under load-balanced routing and a KV$ hit prioritized routing like Bailian on the trace 1. Key insight: schedulers favoring local-tier KV$ hits have session stickiness that we can leverage to balance the workloads. To balance the load while preserving KV$ reuse under a limited global tier, we exploit a new indicator: session information. Specifically, to fully utilize local-tier KV$ hits, the scheduler must prioritize them during scheduling, as schedulers like Bailian do (§ 4.1 ). Our key insight is that under any such KV$-prioritizing scheduler, the placement of a session’s first request determines the placement of all its follow-up requests: a follow-up request mostly reuses the KV$ of earlier turns in the same session (Finding 3.1 ), which typically still resides in the local tier of the instance that served the previous turn (Finding 3.1 ), so the scheduler routes it back to that instance. Hence, if first requests are spread evenly across instances, follow-up requests stay spread as well. Figure 12 confirms this property under the same setup as § 4.1 : under load-balanced routing, only 4.0 % of follow-up requests return to the instance that served the first request. Under Bailian ’s policy, 96.6 % of follow-up requests return to that first-turn instance. Thus, balancing the first requests of sessions ensures a relatively balanced load with high local-tier KV$ hits. ⬇ 1 req = receive () 3 c = kvc . hit_len ( req . prompt ) # KV$, per instance 4 l = load () # load, per instance 5 s = instances . argmax ( c ) # highest KV$ hit 7 if req . turn != 0 \ 8 and not_overloaded ( s , l ) \ 9 and session_not_evicted ( req , s , c ): 10 sched_to = s # stick 11 else : 12 score = load_balance ( req , c , l ) 13 sched_to = instances . rr_argmin ( score ) 15 req . forward ( sched_to ) 17 def not_overloaded ( s , l ): 18 return l [ s ] &lt;= OVERLOAD * mean ( l ) 20 def session_not_evicted ( req , s , c ): 21 # est_hit: hit length estimated from the request’s 22 # conversation history (excludes the new turn); 23 # HIT_RATIO: a constant to tolerate small drifts 24 return c [ s ] &gt; HIT_RATIO * est_hit ( req ) 26 def load_balance ( req , c , l ): 27 # l is parameterized: 28 # PD-colocation: request count 29 # PD-disaggregation: unprefilled tokens 30 return l Figure 13 : Simplified pseudocode of SMetric ’s method. The method: differential balanced session-centric routing. Figure 13 shows how SMetric turns the above insight into our scheduling method: SMetric routes a request based on its position in the session. If the request is the first of its session, i.e., req.turn is 0 0 (line 5), SMetric routes it with the load-balancing-only policy (lines 10–11) to balance sessions across instances. Otherwise, SMetric routes it with a simplified form of KV$-aware routing: it selects the instance with the highest KV$ hit (line 4,8), so requests from the same session stick to the same instance and enjoy local-tier KV$ hits, unless either of two guards fails: not_overloaded() (line 15) or session_not_evicted() (line 20), in which case SMetric falls back to load balancing; we describe the rationale of both guards below. Figure 14 : Distribution of per-window (30s) load imbalance with a fully provisioned global KV$ store. Retrofit for the tail with pre-filter routing (Line 17 in Figure 13 ). The not_overloaded() guard checks whether the target instance is overloaded. We add it because first-request-balanced routing keeps instances well balanced most of the time, but our evaluation shows that tail cases remain: for example, two sessions that start short on the same instance can both grow long, and the instance gradually becomes overloaded. In this case, we need to migrate a long session away to restore the balance, and the fallback load-balancing policy effectively does so. Note that even in this case, SMetric does not sacrifice KV$ reuse, because the global KV$ store lets the new instance fetch the session’s KV$ (➂ in Figure 2 ). We detect such cases by comparing the current load of the instance with the mean load across the cluster, with a hyperparameter ( OVERLOAD ) configuring the overloading threshold. Intuitively, if the current load of the target instance exceeds the mean of the cluster by a threshold, we fall back to a load-balancing-only policy. OVERLOAD typically needs to be larger than 1; our sensitivity analysis in § 5.5 shows that SMetric is quite robust across a common range of values. Figure 14 quantifies the effect of handling the tail (with OVERLOAD=2 ): it plots the per-window (30s) load imbalance with and without the pre-filter ( SMetric vs. SMetric (basic)). The pre-filter noticeably evens the imbalance beyond the 75th percentile of windows. Retrofit for evicted sessions (Line 20 in Figure 13 ). The benefit of sticking requests from the same session to the same instance is to enjoy local-tier KV$ hits, but the local tier can evict the KV$, e.g., when a session idles long enough before its next turn arrives. Though rare, such a request should be treated as the first request of a fresh session, so SMetric balances it across instances. SMetric detects such a case using the actual hit length already computed for routing. Because each request carries its full conversation history through the LLM API [ 35 ] , an unevicted follow-up request should produce a local hit that nearly covers that history. SMetric therefore estimates the hit it expects from the request’s carried history— est_hit , which excludes the freshly appended turn that is never cached—and sticks only when the best actual hit reaches at least HIT_RATIO of this estimate. HIT_RATIO is a hyperparameter below one that absorbs the small drifts between the estimate and the real hit. If the best actual hit falls below HIT_RATIO × \times est_hit , the session’s KV$ has likely been evicted, and SMetric falls back to load balancing. Our ablation (§ 5.5 ) shows SMetric ’s performance is robust to this hyperparameter. The rationale of using the session turn and its availability. SMetric relies on knowing each request’s turn in its session for scheduling (line 7). We choose the turn as the indicator because it can be collected in a router-stateless way—by stateless, we mean the router keeps no record of users’ past requests. A naive alternative is to have the router remember where each session executes—recording, for every active session, the instance it was routed to. This forces the router to maintain a session-to-instance table: the table grows with the number of active sessions, must be kept up to date, and must be garbage-collected as sessions fall idle, since a session never signals its end. Choosing the turn avoids all of this because it can be piggybacked or inferred from the LLM requests, as we describe below. We obtain the turn in this stateless way as follows. One simple option is to modify the agent framework to tag each request with its turn, but this raises compatibility issues: agent frameworks are diverse and evolve quickly (e.g., Claude Code [ 6 ] and OpenClaw [ 36 ] ), and a provider can modify only its own in-house framework, not the external ones its users run. Instead, we infer the turn information: agents interact with LLM serving through standard APIs (e.g., the OpenAI API [ 35 ] ), and since the API follows a stateless design, each request carries the past conversations of the session, so SMetric deduces the turn directly from the number of historical messages in the request. The deduction fails only if the agent drops the history, but the failure is benign: dropping the history forfeits its KV$ reuse, which is uneconomical for the agent and thus rare; even when it happens, SMetric simply treats the request as the first request of a new session—which is the right decision, since such a request hits little KV$ anyway. Performance evaluation Evaluation setup Testbed. Our testbed is a cluster of four GPU servers, each equipped with eight NVIDIA H20 GPUs (96 GB HBM3 per GPU), 160 CPU cores, and 1280 GB of host DRAM. The cluster thus supports up to 32 instances for single-GPU models. Servers are interconnected with 200 Gbps RDMA NICs. Each instance runs the latest vLLM [ 22 , 44 ] with default optimizations enabled and tuned parameters. The global KV$ store is based on Mooncake [ 38 ] and LMCache [ 29 ] , which implement the state-of-the-art global-tier architecture described in Figure 2 . The scheduler runs on a separate CPU server with 64 cores and 256 GB DRAM. Unless otherwise stated, we use all instances to evaluate the performance. Figure 15 : TPS within SLO and per-instance token load for the 32-instance Qwen3-30B-A3B deployment. (a) TPS within SLO under increasing request rates. (b) Inflight token-load heatmaps; each heatmap is normalized by that policy’s p99 token load. Both use a fully provisioned global-tier KV$ store. Evaluated model. We evaluate two models, Qwen3-Coder-30B-A3B and Qwen3-235B-A22B-FP8, both popular models on the market [ 48 ] . Qwen3-30B can be served with only one GPU, allowing us to evaluate up to 32 instances. In contrast, Qwen3-235B requires a minimum of 4 GPUs to serve as an instance, so we can evaluate up to 8 instances. Evaluated workload. We replay Trace 1 (§ 3 ) for all experiments. Because the requests are collected from a much larger cluster than our testbed, we sample them to control the request rate to the cluster, following prior work [ 54 , 58 , 31 , 19 , 41 ] . One issue with replaying is that the evaluated model’s responses differ from those recorded in the trace. Such a difference breaks the KV$ reuse pattern in two ways: the generated response may have a different length, and a follow-up request’s recorded history no longer matches the tokens the serving instance actually cached. We fix the issue as follows. First, we force the evaluated model to generate exactly as many tokens as the recorded response, by ignoring the end-of-sequence token and capping the generation at the recorded length. Second, we rewrite a part of the follow-up requests with the generated content to ensure consistent KV$ reuse. End-to-end serving performance: PD-colocation Figure 16 : (a) Analysis of the TPS for different schedulers with different global-tier offerings. (b) The KV$ reuse ratio and where the reuse comes from. The model: Qwen3-30B-A3B. Compared targets. We compare SMetric with the state-of-the-art schedulers described in § 4.1 . Tokens-per-second comparison. Figure 16 (a) compares the TPS of each scheduler under different global-tier provisionings. The evaluated model is Qwen3-Coder-30B-A3B, served with all 32 instances. We report each method’s maximum TPS within SLO (§ 4.1 ): the maximum cluster-wide TPS, across offered request rates, contributed only by requests that satisfy both the TTFT and TPOT SLOs. The SLOs are set to Bailian ’s production targets. To measure it under each provisioning, we sweep the cluster’s offered request rate and take the maximum TPS within SLO over the sweep; Figure 15 (a) shows one such rate sweep under the fully provisioned global store. SMetric achieves the highest TPS under every provisioning. With a global store, SMetric is 10–16 % higher than the best-performing baseline at each point. Without a global store, SMetric and Bailian are within 1 % of each other: SMetric is slightly higher in this run, but the gap is small because both systems must rely mostly on local-tier hits. Bailian is the strongest baseline across the provisioning sweep. SMetric leads at all these points because it preserves local-tier reuse and load balance at the same time. First, with the global store fully provisioned, SMetric retains a local-tier hit ratio comparable to Bailian —the policy most biased toward local hits—64 % vs. 65 % (Figure 16 (b)). Second, SMetric keeps the load reasonably balanced while preserving local reuse: Figure 15 (b) profiles the per-instance token load, where SMetric ’s mean max-to-mean load ratio (§ 4.1 ) is 2.6 × \times , lower than Bailian (3.0 × \times ) and LMetric (3.2 × \times ), though higher than load-balance-only (2.0 × \times ). Per-token latency. We next analyze the per-token latency, reported as the time-to-first-token (TTFT) and the time-per-output-token (TPOT) following the convention of evaluating LLM serving systems [ 22 , 58 , 46 ] . Figure 17 shows the latency CDFs of each method under the same 32-instance setup, with the fully provisioned global store and the 1.5 × \times request rate in Figure 15 (a). We choose this setup because it favors the baselines: with the fully provisioned store, the strongest baseline reaches a TPS within 15 % of SMetric ’s (Figure 16 (a)), while under smaller stores some baselines lose most of their TPS. Note that unlike the TPS results above, the CDFs count all requests, including those that violate the SLO. For TPOT, SMetric ’s P50 and P90 TPOT are 10 % and 20 % lower than the best-performing baseline at each percentile, respectively, while its P99 TPOT is somewhat higher than Bailian ’s. The P50 and P90 TPOT results corroborate the TPS comparison from the per-request view: intuitively, a cluster with a higher TPS generates each request’s tokens faster and thus lowers most requests’ TPOT. For TTFT, SMetric achieves the lowest latency at all reported percentiles: its median TTFT is 1.1 s, 17 % lower than Bailian , the closest baseline at the median. At P90 and P99, SMetric is also slightly lower than LMetric, the closest baseline at the tail. The reason is that SMetric combines local-tier hits, which skip the prefill of the reused prefix without fetching it from the global tier or recomputing it from scratch, with lower queueing from a more balanced token load (Figure 15 (b)). Figure 17 : Per-token latency distributions for the 32-instance Qwen3-30B-A3B evaluation with a fully provisioned global-tier KV$ store. Figure 18 : TPS within SLO and per-instance token load for the 8-instance Qwen3-235B-A22B-FP8 deployment. (a) TPS within SLO under increasing request rates. (b) Inflight token-load heatmaps; each heatmap is normalized by that policy’s p99 token load. Both use a fully provisioned global-tier KV$ store. Figure 19 : (a) Analysis of the TPS for different schedulers with different global-tier offerings. (b) The KV$ reuse ratio and where the reuse comes from. The model: Qwen3-235B-A22B-FP8. Figure 20 : Per-token latency distributions for the 8-instance Qwen3-235B-A22B-FP8 evaluation with a fully provisioned global-tier KV$ store. Performance with different request rates. Figure 15 (a) also shows how each scheduler behaves as the offered load changes. At low load, the cluster has enough spare capacity that the scheduling choice matters little: at 1.0 × \times load, SMetric is within 1 % of the strongest baseline. The benefit of SMetric appears near the saturation knee, where routing mistakes directly turn into SLO violations, through either load imbalance or underused KV$. At 1.5 × \times load, SMetric improves TPS by 15 % over the strongest baseline shown in the figure; at 1.75 × \times load, the improvement is 4 %. After the cluster is severely overloaded (2.0 × \times load), all schedulers lose TPS. Nevertheless, SMetric drops more slowly than the others. Performance on a larger model. To confirm the effectiveness of SMetric , we further evaluate Qwen3-235B-A22B-FP8, served with eight instances and a fully provisioned global-tier KV$ store. Figure 18 (a) shows the offered-load sweep. SMetric again achieves the highest TPS within SLO: at its peak operating point, it achieves 516 tokens/s. This is 8 % higher than Bailian ’s peak over the sweep, and 20 % higher than LMetric, the strongest baseline at the same operating point. This mirrors the 30B result: the benefit appears near the saturation knee, where routing mistakes directly turn into SLO violations. Figure 18 (b) profiles the per-instance token load at the same peak operating point. SMetric ’s mean max-to-mean load ratio (§ 4.1 ) is 1.6 × \times , close to LMetric (1.6 × \times ) and Bailian (1.6 × \times ), though higher than load-balance-only (1.3 × \times ). The close ratios are partly a setup artifact: this large-model experiment has only eight serving instances, which leaves fewer routing choices and compresses load-skew differences; with 32 instances, Figure 15 (b) shows that SMetric is less skewed than LMetric and Bailian . The load-balanced baseline is the most uniform, but its TPS at this point is 32 % lower than SMetric ’s. The gap arises because a larger model puts more pressure on the global tier’s capacity and bandwidth, so fetching KV$ from it is more likely to be the bottleneck. This pressure makes local-tier reuse important: at the fully provisioned point, SMetric retains a high local-tier hit ratio (69 %) and the highest overall reuse ratio (76 %) (Figure 19 (b)). Figure 20 further shows that SMetric sustains low per-token latency on the larger model. For TTFT, SMetric achieves the lowest latency at all reported percentiles: its median TTFT is 0.85 s, 24 % lower than LMetric, the closest baseline at the median. At P90, SMetric ’s TTFT is 7.7 s, 26 % lower than LMetric, the closest baseline at the tail. For TPOT, SMetric ’s median is within 2 % of Bailian , the best-performing baseline at the median, while its P90 and P99 TPOT are 13 % and 34 % lower than the best-performing baseline at each percentile, respectively. Figure 21 : TPS within SLO and per-instance prefill token load for the 32-instance Qwen3-30B-A3B prefill evaluation under PD disaggregation. (a) Prefill TPS within SLO under increasing request rates. (b) Inflight prefill-token-load heatmaps; each heatmap is normalized by that policy’s p99 token load. Both use a fully provisioned global-tier KV$ store. Figure 22 : (a) Analysis of the TPS (prefill tokens) for different schedulers with different global-tier offerings. (b) The KV$ reuse ratio and where the reuse comes from. The model: Qwen3-30B-A3B under PD disaggregation. Figure 23 : TTFT distributions for the 32-instance Qwen3-30B-A3B evaluation under PD disaggregation with a fully provisioned global-tier KV$ store. End-to-end serving performance: PD-disaggregation Evaluation setup and compared targets. We next evaluate SMetric under PD disaggregation, another common serving paradigm. Since KV$ reuse only affects the prefill computation in PD disaggregation, we focus on the scheduling performance of the prefill instances. Accordingly, unlike the TPS under PD colocation, which counts both prefill and decode tokens, we report the prefill TPS within the SLO: the number of prompt tokens whose KV$ is produced per second, either computed by the prefill or reused from a KV$ hit. We also report the TTFT. The disaggregated cluster is based on vLLM with Mooncake, a state-of-the-art PD-disaggregation serving stack, and also uses LMCache as the global-tier KV$ store. The compared targets are the same as in PD colocation (§ 4.1 ), because scheduling prefill instances faces the same trade-off between local KV$ hits and global load balance. The only exception is load-balance-only: we replace vLLM’s default policy with Dynamo [ 34 ] , a state-of-the-art open-source disaggregated serving system. Dynamo routes a request by the number of new prefill tokens it would compute on each instance. This metric improves on other load indicators (e.g., the batch size used in vLLM) because it also captures KV$ reuse: a hit reduces the new tokens left to prefill. Note that PD colocation cannot directly adopt such a unified token count, because prefill and decode tokens have different computation patterns. Tokens-per-second comparison. Figure 22 (a) compares the prefill TPS of each scheduler when replaying our trace under different global-tier provisionings. The evaluated model is Qwen3-30B-A3B, and we omit the results of a larger model because they are similar. We deploy 32 prefill instances and measure the prefill performance only. Figure 21 (a) further shows one request-rate sweep under the fully provisioned global store. SMetric achieves the highest prefill TPS: it is 2–34 % higher than the best-performing baseline across the global-tier provisioning sweep. For example, at 1.6 × \times Global/Local capacity, SMetric is 15 % higher than Bailian , the strongest baseline at that point; with the fully provisioned global store, it is 2 % higher than LMetric. The improvement can be larger than that under PD colocation. Under PD colocation with the fully provisioned global store, SMetric ’s corresponding TPS gain is 15 % over the strongest baseline (Figure 16 (a)). This larger gain appears because computing the prefill tokens is more sensitive to load imbalance. Specifically, we find that decode tolerates some imbalance in PD colocation: a decode step generates one token per request and demands relatively little computation, so an overloaded instance can still absorb requests into a larger batch. Although a larger batch slows each decode step, it also generates more tokens per step, so load imbalance only mildly degrades the total TPS. Computing the prefill tokens is different: it is compute-intensive, so once the batch saturates the GPU’s compute, further enlarging the batch (or queueing requests behind the current one) only delays the requests without improving the throughput. As a result, an imbalanced load directly costs the prefill TPS. Similar to PD colocation, SMetric outperforms the others because it preserves KV$ reuse and load balance at the same time. For the reuse, Figure 22 (b) shows that SMetric matches the overall reuse ratio of the strongest baselines: 76 % with the fully provisioned global store. Its local-tier hit ratio is also close to Bailian ’s (66 % vs. 68 %), the policy most biased toward local hits. For the load, Figure 21 (b) profiles the per-instance prefill token load, where SMetric ’s mean max-to-mean load ratio (§ 4.1 ) is 3.3 × \times , essentially tied with Bailian (3.3 × \times ) and lower than LMetric (4.2 × \times ) and the load-balanced baseline (4.6 × \times ). Per-token latency. Figure 23 further analyzes the TTFT of each method, measured under the same setup as the TPS comparison above. Since we evaluate prefill instances only, we omit the TPOT. SMetric achieves the lowest TTFT at all percentiles, because it keeps the load balanced without sacrificing KV$ hits. Specifically, at the median, SMetric ’s TTFT (1.1 s) is 37 % lower than that of Bailian (1.8 s), the best-performing baseline at this percentile. At the tail, the closest baselines become the load-balanced baseline at P90 and LMetric at P99: SMetric ’s P90 TTFT (10.9 s) is 18 % lower than the load-balanced baseline’s (13.3 s), and its P99 TTFT (44.2 s) is 7 % lower than LMetric’s (47.4 s). The shift of the closest baseline echoes the PD colocation results (§ 5.2 ): the median TTFT is dominated by KV$ hits, while the tail TTFT is dominated by queueing. Impact of pre-filter routing Figure 24 : Impact of pre-filter routing in the 32-instance Qwen3-30B-A3B evaluation with a fully provisioned global-tier KV$ store. The top plot sweeps the offered load and reports good TPS. The bottom plots report complementary CDFs for TTFT and TPOT, with a log-scale y-axis for the tail probability. Figure 24 isolates the effect of pre-filter routing under the same setup as the latency evaluation in § 5.2 . We compare SMetric with SMetric (basic), which removes the two pre-filter guards described in Figure 13 . The top plot shows that pre-filter routing gives a modest good-TPS gain across the paired load points. The bottom plots show the main benefit at the per-request level by plotting tail probability on a log scale, which exposes the p90-and-above latency region that a standard CDF compresses near 1. For these latency samples, SMetric ’s P90 and P99 TTFTs are 12.1 s and 45.3 s, lower than SMetric (basic)’s 16.1 s and 51.2 s; the TPOT tail is closer, but SMetric still lowers P95 TPOT (82.5 ms vs. 89.2 ms). This explains the modest throughput improvement: the pre-filter guards primarily reduce tail queueing under imbalance. Sensitivity to pre-filter routing parameters Figure 25 : Sensitivity of SMetric ’s pre-filter routing parameters for the 32-instance Qwen3-30B-A3B evaluation with a fully provisioned global-tier KV$ store. (a) Sweeping the OVERLOAD parameter in the not_overloaded() guard. (b) Sweeping the HIT_RATIO parameter in the session_not_evicted() guard. In each panel, stacked bars break down the local/global KV$ hit ratio, and the line reports TPS within SLO. Figure 25 further studies the impact of different hyperparameters described in Figure 13 . The setup is the same as the latency evaluation in § 5.2 . Across OVERLOAD values from 1.0 to ∞ \infty , SMetric ’s TPS stays within 6 % of the default and the overall hit ratio stays at 75–76 %. Across HIT_RATIO values from 0 to 0.75, SMetric ’s TPS stays within 4 % of the default and the overall hit ratio also stays at 75–76 %. The default settings therefore sit on a broad performance plateau: the pre-filter guards avoid tail overload and avoid sticking to sessions whose local cache is likely evicted. Related Work LLM request scheduling. Recent work on LLM serving has explored request scheduling from multiple perspectives, including KV$-aware and SLO-aware scheduling, dynamic rescheduling with request migration, and flexible request partitioning across instances [ 42 , 38 , 43 , 59 , 11 , 40 ] . Our work builds upon these insights and mechanisms, and further derives agent-session-aware hints for the scheduler to improve serving performance for agents. Unlike systems that co-design serving with agent applications and require applications to expose inter-request structure [ 26 , 30 ] , SMetric operates entirely on the provider side, treating agents as unmodified clients. This matches how LLM providers, including Bailian , serve agents today. Serving KV$ efficiently. Reusing KV$ across requests has been widely studied to avoid recomputing KV tensors for LLM serving, and both industry and academia have built mature system mechanisms for supporting KV$ reuse [ 15 , 16 , 22 , 49 , 50 , 57 , 52 ] . We build upon the mechanism of KV$ reuse, including multi-level KV$ store, while providing a systematic analysis and optimizations of the scheduling aspects for better utilization of the multi-level KV$ store. Other KV$-related optimizations. Besides cross-request reuse, recent work also optimizes the memory footprint, transfer cost, and placement of KV$ with a system–algorithms co-design. For example, CacheGen compresses and streams KV states to reduce KV$ transfer latency and bandwidth consumption [ 27 , 23 , 18 ] . KV quantization and compression methods reduce KV$ memory pressure through low-bit quantization, token pruning, layer-wise allocation or unified compression [ 28 , 20 , 24 , 10 , 56 ] . Despite their potential performance advantages for KV$ reuse, these techniques compromise model accuracy, which to the best of our knowledge has limited their adoption in production serving. Nevertheless, these optimizations can strengthen several aspects of our session-centric balanced design if applied. For example, with a largely compressed KV$ size, GPU cache hits become more important, and SMetric can achieve them in a load-balanced way. Optimizing LLM serving. Beyond cluster-level scheduling and KV$ management, recent work has optimized LLM serving at different layers of the stack. For example, DeepSeek-V3 and DeepSeek-V4 reduce long-context inference cost through algorithm-architecture co-designs such as MLA and compressed sparse attention [ 12 , 13 ] , FlashInfer [ 51 ] improves attention execution through customizable kernels and optimized KV layouts, BlitzScale and Aegaeon improve elasticity and resource scaling for serving workloads [ 48 , 55 ] , and many others [ 21 , 51 , 25 , 22 ] . These optimizations are orthogonal to SMetric . Conclusion Agentic serving shifts the goal of LLM scheduling to cluster-wide TPS and makes KV$ reuse dominant. Our study of two real-world traces shows that existing schedulers trade load balance for KV$ reuse, and that this trade is unnecessary: the global-tier KV$ store decouples reuse from request placement, and the workload’s intra-session locality means balancing each session’s first request suffices to balance the cluster. SMetric turns these insights into balanced session-centric scheduling—first requests scheduled for balance, follow-up requests routed cache-aware—and outperforms state-of-the-art schedulers: 10–16 % higher TPS under prefill-decode colocation with a global store and 2–34 % higher prefill TPS under disaggregation across global-tier provisionings. References [1] A. Agrawal, N. Kedia, A. Panwar, J. Mohan, N. Kwatra, B. Gulavani, A. Tumanov, and R. Ramjee (2024) Taming Throughput-Latency tradeoff in LLM inference with Sarathi-Serve . In 18th USENIX Symposium on Operating Systems Design and Implementation (OSDI 24) , Santa Clara, CA , pp. 117–134 . External Links: ISBN 978-1-939133-40-3 , Link Cited by: §2 . [2] (2025) AIBrix . Note: https://github.com/vllm-project/aibrix Cited by: §1 , §4.1 . [3] (2025) AIGW . Note: https://github.com/aigw-project/aigw Cited by: 1st item , §4.1 . [4] Anthropic (2024) Claude code: sub-agents and agent teams . Note: https://docs.anthropic.com/en/docs/claude-code/sub-agents Cited by: §3.1 . [5] Anthropic (2025) Building a C compiler with Claude . Note: https://www.anthropic.com/engineering/building-c-compiler Cited by: §1 . [6] Anthropic (2026) Claude code . Note: https://code.claude.com/docs/en/overview Cited by: §1 , §2 , §3.1 , §4.2 . [7] Anthropic (2026) Elevated error rates on Opus 4.6 . Note: Anthropic Status Page External Links: Link Cited by: §1 , §4.1 . [8] I. Arapakis, X. Bai, and B. B. Cambazoglu (2014) Impact of response latency on user behavior in web search . In The 37th International ACM SIGIR Conference on Research and Development in Information Retrieval, SIGIR ’14, Gold Coast , QLD, Australia - July 06 - 11, 2014 , S. Geva, A. Trotman, P. Bruza, C. L. A. Clarke, and K. Järvelin (Eds.) , pp. 103–112 . External Links: Link , Document Cited by: §1 . [9] L. Bai, Z. Huang, X. Wang, J. Sun, R. Mihalcea, E. Brynjolfsson, A. Pentland, and J. Pei (2026) How do ai agents spend your money? analyzing and predicting token consumption in agentic coding tasks . External Links: 2604.22750 , Link Cited by: §1 . [10] Z. Cai, Y. Zhang, B. Gao, Y. Liu, T. Liu, K. Lu, W. Xiong, Y. Dong, B. Chang, J. Hu, and W. Xiao (2024) PyramidKV: dynamic KV cache compression based on pyramidal information funneling . CoRR abs/2406.02069 . External Links: Link , Document , 2406.02069 Cited by: §6 . [11] S. Chen, Z. Jia, S. Khan, A. Krishnamurthy, and P. B. Gibbons (2025) SLOs-serve: optimized serving of multi-slo llms . CoRR abs/2504.08784 . External Links: Link , Document , 2504.08784 Cited by: §4.1 , §6 . [12] DeepSeek-AI (2024) DeepSeek-v3 technical report . CoRR abs/2412.19437 . External Links: Link , Document , 2412.19437 Cited by: §6 . [13] DeepSeek-AI (2026) DeepSeek-v4: towards highly efficient million-token context intelligence . Note: Technical report Available at Hugging Face model repository. Accessed: 2026-06-10 External Links: Link Cited by: §6 . [14] H. Ding, Z. Wang, and H. Chen (2026) FM-agent: scaling formal methods to large systems via llm-based hoare-style reasoning . External Links: arXiv:2604.11556 Cited by: §1 . [15] B. Gao, Z. He, P. Sharma, Q. Kang, D. Jevdjic, J. Deng, X. Yang, Z. Yu, and P. Zuo (2024) Cost-efficient large language model serving for multi-turn conversations with cachedattention . In Proceedings of the 2024 USENIX Annual Technical Conference, USENIX ATC 2024, Santa Clara, CA, USA, July 10-12, 2024 , S. Bagchi and Y. Zhang (Eds.) , pp. 111–126 . External Links: Link Cited by: §6 . [16] I. Gim, G. Chen, S. Lee, N. Sarda, A. Khandelwal, and L. Zhong (2024) Prompt cache: modular attention reuse for low-latency inference . In Proceedings of the Seventh Annual Conference on Machine Learning and Systems, MLSys 2024, Santa Clara, CA, USA, May 13-16, 2024 , P. B. Gibbons, G. Pekhimenko, and C. D. Sa (Eds.) , External Links: Link Cited by: §6 . [17] Google (2026) Llm-d . Note: https://github.com/llm-d/llm-d Cited by: §1 , §4.1 . [18] N. Gopal and K. Kaffes (2026) Harvest: opportunistic peer-to-peer GPU caching for LLM inference . CoRR abs/2602.00328 . External Links: Link , Document , 2602.00328 Cited by: §6 . [19] A. Gujarati, R. Karimi, S. Alzayat, W. Hao, A. Kaufmann, Y. Vigfusson, and J. Mace (2020) Serving dnns like clockwork: performance predictability from the bottom up . In 14th USENIX Symposium on Operating Systems Design and Implementation, OSDI 2020, Virtual Event, November 4-6, 2020 , pp. 443–462 . External Links: Link Cited by: §5.1 . [20] C. Hooper, S. Kim, H. Mohammadzadeh, M. W. Mahoney, Y. S. Shao, K. Keutzer, and A. Gholami (2024) KVQuant: towards 10 million context length LLM inference with KV cache quantization . In Advances in Neural Information Processing Systems 37: Annual Conference on Neural Information Processing Systems 2024, NeurIPS 2024, Vancouver, BC, Canada, December 10 - 15, 2024 , A. Globersons, L. Mackey, D. Belgrave, A. Fan, U. Paquet, J. M. Tomczak, and C. Zhang (Eds.) , External Links: Link Cited by: §6 . [21] H. Kang, S. Bharadwaj, J. Hensman, T. Krishna, V. Rühle, and S. Rajmohan (2025) TurboAttention: efficient attention approximation for high throughputs llm . In Proceedings of the Eighth Conference on Machine Learning and Systems, MLSys 2025, Santa Clara, CA, USA, May 12-15, 2025 , M. Zaharia, G. Joshi, and Y. (. Lin (Eds.) , External Links: Link Cited by: §6 . [22] W. Kwon, Z. Li, S. Zhuang, Y. Sheng, L. Zheng, C. H. Yu, J. Gonzalez, H. Zhang, and I. Stoica (2023) Efficient memory management for large language model serving with pagedattention . In Proceedings of the 29th Symposium on Operating Systems Principles, SOSP 2023, Koblenz, Germany, October 23-26, 2023 , pp. 611–626 . External Links: Link , Document Cited by: §5.1 , §5.2 , §6 , §6 . [23] R. Li, S. Pal, V. N. Pullu, P. Sinha, J. Ryoo, L. K. John, and N. J. Yadwadkar (2025) MIRAGE: KV cache optimization through parameter remapping for multi-tenant LLM serving . CoRR abs/2507.11507 . External Links: Link , Document , 2507.11507 Cited by: §6 . [24] Y. Li, Y. Huang, B. Yang, B. Venkitesh, A. Locatelli, H. Ye, T. Cai, P. Lewis, and D. Chen (2024) SnapKV: LLM knows what you are looking for before generation . In Advances in Neural Information Processing Systems 37: Annual Conference on Neural Information Processing Systems 2024, NeurIPS 2024, Vancouver, BC, Canada, December 10 - 15, 2024 , A. Globersons, L. Mackey, D. Belgrave, A. Fan, U. Paquet, J. M. Tomczak, and C. Zhang (Eds.) , External Links: Link Cited by: §6 . [25] D. Liakopoulos, T. Hu, P. Sinha, and N. J. Yadwadkar (2025) IServe: an intent-based serving system for llms . CoRR abs/2501.13111 . External Links: Link , Document , 2501.13111 Cited by: §6 . [26] C. Lin, Z. Han, C. Zhang, Y. Yang, F. Yang, C. Chen, and L. Qiu (2024) Parrot: efficient serving of llm-based applications with semantic variable . In 18th USENIX Symposium on Operating Systems Design and Implementation, OSDI 2024, Santa Clara, CA, USA, July 10-12, 2024 , A. Gavrilovska and D. B. Terry (Eds.) , pp. 929–945 . External Links: Link Cited by: §6 . [27] Y. Liu, H. Li, Y. Cheng, S. Ray, Y. Huang, Q. Zhang, K. Du, J. Yao, S. Lu, G. Ananthanarayanan, M. Maire, H. Hoffmann, A. Holtzman, and J. Jiang (2024) CacheGen: KV cache compression and streaming for fast large language model serving . In Proceedings of the ACM SIGCOMM 2024 Conference, ACM SIGCOMM 2024, Sydney, NSW, Australia, August 4-8, 2024 , pp. 38–56 . External Links: Link , Document Cited by: §6 . [28] Z. Liu, J. Yuan, H. Jin, S. (. Zhong, Z. Xu, V. Braverman, B. Chen, and X. Hu (2024) KIVI: A tuning-free asymmetric 2bit quantization for KV cache . In Forty-first International Conference on Machine Learning, ICML 2024, Vienna, Austria, July 21-27, 2024 , R. Salakhutdinov, Z. Kolter, K. A. Heller, A. Weller, N. Oliver, J. Scarlett, and F. Berkenkamp (Eds.) , Proceedings of Machine Learning Research , pp. 32332–32344 . External Links: Link Cited by: §6 . [29] (2026) LMCache: the best kv cache layer for enterprise-scale llm inference . Note: https://github.com/LMCache/LMCache Cited by: §1 , §4.1 , §5.1 . [30] M. Luo, X. Shi, C. Cai, T. Zhang, J. Wong, Y. Wang, C. Wang, Y. Huang, Z. Chen, J. E. Gonzalez, and I. Stoica (2025) Autellix: an efficient serving engine for LLM agents as general programs . CoRR abs/2502.13965 . External Links: Link , Document , 2502.13965 Cited by: §6 . [31] X. Miao, C. Shi, J. Duan, X. Xi, D. Lin, B. Cui, and Z. Jia (2024) SpotServe: serving generative large language models on preemptible instances . In Proceedings of the 29th ACM International Conference on Architectural Support for Programming Languages and Operating Systems, Volume 2, ASPLOS 2024, La Jolla, CA, USA, 27 April 2024- 1 May 2024 , R. Gupta, N. B. Abu-Ghazaleh, M. Musuvathi, and D. Tsafrir (Eds.) , pp. 1112–1127 . External Links: Link , Document Cited by: §5.1 . [32] MiniMax (2026) MiniMax-M3 . Note: https://www.minimax.io/blog/minimax-m3 Cited by: §1 . [33] Moonshot AI (2026) Kimi introduces agent swarm: let 100 AI agents work for you . Note: https://www.kimi.com/blog/agent-swarm Cited by: §3.1 . [34] NVIDIA (2025) Ai-dynamo . Note: https://github.com/ai-dynamo/dynamo Cited by: 1st item , §4.1 , §5.3 . [35] OpenAI (2026) OpenAI API . Note: https://openai.com/api/ Cited by: §4.2 , §4.2 . [36] OpenClaw (2026) OpenClaw . Note: https://openclaw.ai Cited by: §1 , §3.1 , §4.2 . [37] P. Patel, E. Choukse, C. Zhang, A. Shah, Í. Goiri, S. Maleki, and R. Bianchini (2024) Splitwise: efficient generative LLM inference using phase splitting . In 51st ACM/IEEE Annual International Symposium on Computer Architecture, ISCA 2024, Buenos Aires, Argentina, June 29 - July 3, 2024 , pp. 118–132 . External Links: Link , Document Cited by: §2 . [38] R. Qin, Z. Li, W. He, J. Cui, F. Ren, M. Zhang, Y. Wu, W. Zheng, and X. Xu (2025) Mooncake: trading more storage for less computation — a KVCache-centric architecture for serving LLM chatbot . In 23rd USENIX Conference on File and Storage Technologies (FAST 25) , Santa Clara, CA , pp. 155–170 . External Links: ISBN 978-1-939133-45-8 , Link Cited by: §2 , §4.1 , §4.1 , §4.1 , §5.1 , §6 . [39] Qwen Team (2026) Qwen3.7 . Note: https://qwen.ai/blog?id=qwen3.7 Cited by: §1 . [40] C. Ruan, Y. Chen, D. Tian, Y. Shi, Y. Wu, J. Li, and C. Li (2025) DynaServe: unified and elastic tandem-style execution for dynamic disaggregated LLM serving . CoRR abs/2504.09285 . External Links: Link , Document , 2504.09285 Cited by: §6 . [41] S. M. Sajal, T. Zhu, B. Urgaonkar, and S. Sen (2024) TraceUpscaler: upscaling traces to evaluate systems at high load . In Proceedings of the Nineteenth European Conference on Computer Systems, EuroSys 2024, Athens, Greece, April 22-25, 2024 , pp. 942–961 . External Links: Link , Document Cited by: §5.1 . [42] V. Srivatsa, Z. He, R. Abhyankar, D. Li, and Y. Zhang (2025) Preble: efficient distributed prompt scheduling for LLM serving . In The Thirteenth International Conference on Learning Representations, ICLR 2025, Singapore, April 24-28, 2025 , External Links: Link Cited by: §1 , §2 , §4.1 , §6 . [43] B. Sun, Z. Huang, H. Zhao, W. Xiao, X. Zhang, Y. Li, and W. Lin (2024) Llumnix: dynamic scheduling for large language model serving . In 18th USENIX Symposium on Operating Systems Design and Implementation, OSDI 2024, Santa Clara, CA, USA, July 10-12, 2024 , A. Gavrilovska and D. B. Terry (Eds.) , pp. 173–191 . External Links: Link Cited by: §2 , §6 . [44] (2025) VLLM v0.12.0 release . Note: https://github.com/vllm-project/vllm/releases/tag/v0.12.0 Cited by: §1 , 3rd item , §4.1 , §5.1 . [45] J. Wang, J. Han, X. Wei, S. Shen, D. Zhang, C. Fang, R. Chen, W. Yu, and H. Chen (2025) KVCache cache in the wild: characterizing and optimizing kvcache cache at a large cloud provider . In 2025 USENIX Annual Technical Conference (USENIX ATC 25) , External Links: Link Cited by: §1 , §2 . [46] B. Wu, S. Liu, Y. Zhong, P. Sun, X. Liu, and X. Jin (2024) LoongServe: efficiently serving long-context large language models with elastic sequence parallelism . In Proceedings of the ACM SIGOPS 30th Symposium on Operating Systems Principles, SOSP 2024, Austin, TX, USA, November 4-6, 2024 , E. Witchel, C. J. Rossbach, A. C. Arpaci-Dusseau, and K. Keeton (Eds.) , pp. 640–654 . External Links: Link , Document Cited by: §4.1 , §5.2 . [47] Y. Wu, S. Chen, Y. Zhong, R. Huang, Y. Tan, W. Zhang, L. Zhang, S. Zhou, Y. Liu, S. Zhou, M. Zhang, X. Jin, and P. Huang (2026) DualPath: breaking the storage bandwidth bottleneck in agentic LLM inference . CoRR abs/2602.21548 . External Links: Link , Document , 2602.21548 Cited by: §2 . [48] Y. Xiang, X. Li, K. Qian, Y. Yang, D. Zhu, W. Yu, E. Zhai, X. Liu, X. Jin, and J. Zhou (2025) Aegaeon: effective GPU pooling for concurrent LLM serving on the market . In Proceedings of the ACM SIGOPS 31st Symposium on Operating Systems Principles, SOSP 2025, Lotte Hotel World, Seoul, Republic of Korea, October 13-16, 2025 , Y. Won, Y. Kwon, D. Yuan, and R. Isaacs (Eds.) , pp. 1030–1045 . External Links: Link , Document Cited by: §5.1 , §6 . [49] J. Yao, H. Li, Y. Liu, S. Ray, Y. Cheng, Q. Zhang, K. Du, S. Lu, and J. Jiang (2025) CacheBlend: fast large language model serving for RAG with cached knowledge fusion . In Proceedings of the Twentieth European Conference on Computer Systems, EuroSys 2025, Rotterdam, The Netherlands, 30 March 2025 - 3 April 2025 , pp. 94–109 . External Links: Link , Document Cited by: §6 . [50] L. Ye, Z. Tao, Y. Huang, and Y. Li (2024) ChunkAttention: efficient self-attention with prefix-aware KV cache and two-phase partition . In Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers), ACL 2024, Bangkok, Thailand, August 11-16, 2024 , L. Ku, A. Martins, and V. Srikumar (Eds.) , pp. 11608–11620 . External Links: Link , Document Cited by: §6 . [51] Z. Ye, L. Chen, R. Lai, W. Lin, Y. Zhang, S. Wang, T. Chen, B. Kasikci, V. Grover, A. Krishnamurthy, and L. Ceze (2025) FlashInfer: efficient and customizable attention engine for LLM inference serving . In Proceedings of the Eighth Conference on Machine Learning and Systems, MLSys 2025, Santa Clara, CA, USA, May 12-15, 2025 , M. Zaharia, G. Joshi, and Y. (. Lin (Eds.) , External Links: Link Cited by: §6 . [52] L. Yu and J. Li (2023) Stateful large language model serving with pensieve . CoRR abs/2312.05516 . External Links: Link , Document , 2312.05516 Cited by: §6 . [53] Y. Yuan, P. Zuo, B. Wang, Z. Chen, Z. Tan, and Z. Yu (2026) DualMap: enabling both cache affinity and load balancing for distributed LLM serving . CoRR abs/2602.06502 . External Links: Link , Document , 2602.06502 Cited by: §1 , §4.1 . [54] D. Zhang, J. Han, K. Zhang, X. Wei, S. Shen, C. Fang, W. Yu, J. Zhou, and R. Chen (2026) Simple is better: multiplication may be all you need for llm request scheduling . In Proceedings of the 20th USENIX Symposium on Operating Systems Design and Implementation (OSDI ’26) , Seattle, WA, USA . External Links: Link Cited by: §1 , §1 , §2 , 2nd item , §4.1 , §4.1 , §5.1 . [55] D. Zhang, H. Wang, Y. Liu, X. Wei, Y. Shan, R. Chen, and H. Chen (2025) BlitzScale: fast and live large model autoscaling with O(1) host caching . In 19th USENIX Symposium on Operating Systems Design and Implementation, OSDI 2025, Boston, MA, USA, July 7-9, 2025 , L. Zhou and Y. Zhou (Eds.) , pp. 275–293 . External Links: Link Cited by: §4.1 , §6 . [56] Y. Zhang, Y. Hu, R. Zhao, J. C. S. Lui, and H. Chen (2024) Unifying KV cache compression for large language models with leankv . CoRR abs/2412.03131 . External Links: Link , Document , 2412.03131 Cited by: §6 . [57] L. Zheng, L. Yin, Z. Xie, J. Huang, C. Sun, C. H. Yu, S. Cao, C. Kozyrakis, I. Stoica, J. E. Gonzalez, C. W. Barrett, and Y. Sheng (2023) Efficiently programming large language models using sglang . CoRR abs/2312.07104 . External Links: Link , Document , 2312.07104 Cited by: §6 . [58] Y. Zhong, S. Liu, J. Chen, J. Hu, Y. Zhu, X. Liu, X. Jin, and H. Zhang (2024) DistServe: disaggregating prefill and decoding for goodput-optimized large language model serving . In 18th USENIX Symposium on Operating Systems Design and Implementation, OSDI 2024, Santa Clara, CA, USA, July 10-12, 2024 , pp. 193–210 . External Links: Link Cited by: §1 , §2 , §4.1 , §5.1 , §5.2 . [59] K. Zhu, H. Shi, L. Xu, J. Shan, A. Krishnamurthy, B. Kasikci, and L. Xie (2025) PolyServe: efficient multi-slo serving at scale . CoRR abs/2507.17769 . External Links: Link , Document , 2507.17769 Cited by: §1 , §1 , §2 , §4.1 , §6 . Experimental support, please view the build logs for errors. Generated by L A T E xml . Instructions for reporting errors We are continuing to improve HTML versions of papers, and your feedback helps enhance accessibility and mobile support. To report errors in the HTML that will help us improve conversion and rendering, choose any of the methods listed below: Click the "Report Issue" ( ) button, located in the page header. Tip: You can select the relevant text first, to include it in your report. Our team has already identified the following issues . We appreciate your time reviewing and reporting rendering errors we may not have found yet. Your efforts will help us improve the HTML versions for all readers, because disability should not be a barrier to accessing research. Thank you for your continued support in championing open access for all. Have a free development cycle? Help support accessibility at arXiv! Our collaborators at LaTeXML maintain a list of packages that need conversion , and welcome developer contributions . We gratefully acknowledge support from our major funders , member institutions , , and all contributors. About &middot; Help &middot; Contact &middot; Subscribe &middot; Copyright &middot; Privacy &middot; Accessibility &middot; Operational Status (opens in new tab) Major funding support from
